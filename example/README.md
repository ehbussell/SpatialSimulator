# Individual Simulator Basic Example

## Usage
This setup runs a single epidemic on 1000 hosts with two initially infected hosts.  To run from the parent directory, use:

`python .\simulator.py -c example\config.ini`
